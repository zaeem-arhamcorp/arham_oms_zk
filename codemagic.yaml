workflows:
  ios-release:
    name: iOS Release Build

    max_build_duration: 120

    environment:
      flutter: stable
      xcode: latest

    scripts:
      - flutter clean
      - flutter pub get
      - xcode-project use-profiles
      - flutter build ipa --release

    artifacts:
      - build/ios/ipa/*.ipa